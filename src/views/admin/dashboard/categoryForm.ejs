

<!DOCTYPE html>
<html lang="es">
	<%- include("../../template/head") %>

	<body id="page-top">
		<!-- Navigation-->
		<%- include("../../components/NavBarComponent") %>

		<section class="page-section bg-light" id="portfolio">
			<div class="container">
				<div class="d-flex justify-content-between align-items-center">
					<h3 class="mt-3">
						<% if (data) { %> Editar categoria <% } else { %> Añadir categoria
						<% } %>
					</h3>

				</div>
				<div class="">
					<%- include('../../components/categoryFormComponent',{data,
					RoutesLinks}) %>
				</div>
			</div>
		</section>

		<%- include("../../template/scripts") %>
	</body>
</html>
